spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/httpepe
    username: httpepe
    password: httpepe

server:
  error:
    include-message: always
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE